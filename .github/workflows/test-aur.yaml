name: Test publishing AUR package

on:
  - push

jobs:
  publish_aur_package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Publish AUR package
        uses: ./
        with:
          pkgname: test-publishing-aur-package-using-github-action-0
          pkgbuild: ./PKGBUILD
          commit_username: ${{ secrets.TEST_AUR_USERNAME }}
          commit_email: ${{ secrets.TEST_AUR_EMAIL }}
          ssh_private_key: ${{ secrets.TEST_AUR_SSH_PRIVATE_KEY }}
